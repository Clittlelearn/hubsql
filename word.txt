我修改了src/model/ 交易模型，使用的是utxo 的账户模模型，并将所有需要的字段全部序列化了，其他的字段无关紧要不需要关心
print_block_0_35.json 有此链的块结构数据，现在最总要的一步是正确的解析出所有账户的余额，并存入到msql （新建一张用于查询的余额的表）
由于是UTXO的账户模型，因此解析块的时候需要用到 KV 数据库存储和删除UTXO ，就用rocksdb。




## Get
http://127.0.0.1:13134/GetBlockHeight 


##Post

http://127.0.0.1:13134/GetBlockByHeight

{
  "id": "1",
  "jsonrpc": "2.0",
  "method": "GetBlockByHeight",
  "params": {
    "begin": 0,
    "end": 2
  }
}

删除数据库中的旧质押表
创建一张表负责存储质押数据，需要字段质：
    押金额，
    质押时间，
    是否已经解质押（如果解质押不删除记录只是将这个字段标注为解质押）
    佣金率（commissionRate）
    质押类型
    质押hash
完成质押解质押的解析，并接入REST API



删除数据库中的旧投资表
创建一张表负责存储投资数据，需要字段质：
    投资金额，
    投资时间，
    是否已经解投资（如果解投资不删除记录只是将这个字段标注为解投资）
    bonusAddr
    投资类型
    投资hash
    

添加注册投资解投资的解析 添加注册投资解投资的存储，并接入REST API


投资的区块在20 21 高度，解投资在22高度




提案、投票这两个业务高度相关
新建业务表提案，和投票

提案表需要的字段：
    提案hash
    交易信息（用于存储提案交易的txinfo）
    提案投票的数量
    是否已经撤销提案（同样撤销提案不擅长只是标记为已撤销）
    是否是第一笔提案（OHI提案）

投票表需要的字段：
    投票交易的hash
    被投票交易的hash（提案hash，第一步提案则是OHI）
    被投票交易的类型
    投票类型


添加注册提案和投票的解析 添加注册提案和投票的存储，接入RSET API

需要注意的是链上的第一个提案的资产会被视为OHI（资产名），之后提案的资产则使用提案hash作为资产名
在投票解析的时候需要注意一下


创建锁定业务表
字段：
    锁定交易hash
    锁定资产类型（hash或OHI）
    锁定金额
    锁定类型
    是否已经解锁定

创建交易记录表：
字段：
    交易hash
    交易类型
    交易utxo（json格式存储）

创建合约交易表：
字段：
    交易hash
    部署还是执行
    是否是合约跃入
    是否是合约跃出
    跃入金额
    跃出金额
    跃入跃出资产类类型
    txInfo （json格式）
    以及你认为必要的字段

需要注意的是跃入跃出是特殊的合约执行交易
交易记录只保存最新20个高度

11高度跃入
27高度跃出
高度1-6 为合约部署
7高度合约调用
23高度锁定
24高度解锁定
25高度解锁定


创建申领表：
    字段：
        申领交易hash
        申领金额
        申领资产类型

35高度申领
创建并注册申领解析和申领存储





现在有个需求是查询余额需要3种方式：
    第一种是查询第一个提案到本链的资产也就是OHI
    第二种是其他提案跃入到本链的资产（hash assettype）
    第三种是ERC20合约里的资产（通过部署时的参数确定余额，还有合约调用时通过参数确定转账的金额）
    因此要从新设计多张余额表
还有有个需求是用户可以通过post请求将账号与链上部署的erc20合约关联起来，以通过第三种方式查询该代币的余额
    也需要设计数据库表

我在项目里放入了HypERC20 和 LockableToken5.sol 



我已经授权你完全控制
现在在/home/wbl/下有一个前端项目hubfrontend 它有几个业务：
    查询余额：
        包括OHI（第一个提案资产）、其他提案资产、ERC20合约资产（token），除了默认OHI之外，其他资产类型可以由账户通过添加token（数据库表account_erc20_contracts）来关联到链上已经部署的REC20
        合约（token）和提案 来显示对应余额，对应数据库 erc20_balances （erc20 合约资产表）和 proposal_asset_balances（提案资产表）
    显示validators:
        validators 就是已经质押和投资过度的节点
    显示claim记录：
        数据库表 claim_records
    flowIn 资产类型表：
        已经跃入的资产类型列表：
    提案表：
        提案和投票详情
    
需要重新适配一下接口和hubsql对应上
添加一个调试模式，在输入公钥的情况下就可以查询相关信息而不用连接钱包







